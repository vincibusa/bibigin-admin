rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection - users can create and read/write their own profile
    match /users/{userId} {
      // Allow creation during registration (when user is authenticated but document doesn't exist yet)
      allow create: if request.auth != null && request.auth.uid == userId;
      // Allow users to read and update their own profile
      allow read, update: if request.auth != null && request.auth.uid == userId;
      // Allow admins to read/write any user profile
      allow read, write: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Products collection - read by all, write by authenticated admin users
    match /products/{productId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Orders collection - read/write by authenticated admin users only
    match /orders/{orderId} {
      allow read, write: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Customers collection - read/write by authenticated admin users only
    match /customers/{customerId} {
      allow read, write: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Analytics/Stats collection - read by authenticated admin users only
    match /analytics/{document=**} {
      allow read: if request.auth != null && request.auth.token.admin == true;
      allow write: if false; // Analytics should be read-only
    }
    
    // Admin users collection - read/write by authenticated admin users only
    match /admin_users/{userId} {
      allow read, write: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
